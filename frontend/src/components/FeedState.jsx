export default function FeedState({status}){if(!status)return null;const label=status.mode==='live'?'Live':status.mode==='stale'?'Stale':'Waiting for first sync';return <div className="mb-3 flex items-center justify-between text-xs text-slate-500"><span className={`rounded-full border px-2 py-1 ${status.mode==='live'?'border-slate-200':status.mode==='stale'?'border-red-200 text-red-600':'border-slate-200'}`}>{label}</span>{status.updated_at&&<span className="mono">Updated {new Date(status.updated_at).toLocaleString()}</span>}</div>}